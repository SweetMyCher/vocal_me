<%= form_with(model: user_text) do |form| %>
  <% if user_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_text.errors.count, "error") %> prohibited this user_text from being saved:</h2>

      <ul>
        <% user_text.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <% text_id =  @text ? @text.id : nil %>
    <%= form.label :custom_text %>
    <% if @text%>
      <%= form.text_area :custom_text, size: '50x50', value: @text.text %>
    <% else %>
      <%= form.text_area :custom_text, size: '50x50' %>
    <% end %>
  </div>

  <%= form.hidden_field :text_id, value: text_id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
